# Optional Blueprint. Dashboard-only users: paste the buildCommand line into Render Build Command.
services:
  - type: web
    runtime: python
    buildCommand: bash scripts/render-pip-install.sh
    # Must bind $PORT. Never use GeventWebSocketWorker unless gevent-websocket is installed (see scripts/render-start.sh).
    startCommand: bash scripts/render-start.sh
