# ── Core Web Framework ─────────────────────────────────────────────────────────
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6
python-socketio==5.10.0
python-engineio==4.8.0
flask-sqlalchemy==3.1.1

# ── ML / Data Science ──────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.1.4
scikit-learn==1.4.2
joblib==1.3.2
# XGBoost (optional but recommended — falls back to sklearn GBM if not available)
# xgboost==2.0.3

# ── Transformers (FinBERT + DistilRoBERTa) ─────────────────────────────────────
# Uncomment if you have enough RAM (4GB+ recommended)
# torch==2.2.0
# transformers==4.39.0

# ── NLP Baselines ─────────────────────────────────────────────────────────────
vaderSentiment==3.3.2
textblob==0.17.1
nltk==3.8.1

# ── News & Data Fetching ───────────────────────────────────────────────────────
feedparser==6.0.11
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3

# ── Social Sentiment ──────────────────────────────────────────────────────────
# praw==7.7.1            # Reddit official API (optional, uses public JSON without this)
# tweepy==4.14.0         # Twitter API v2 (requires developer account)
pytrends==4.9.2          # Google Trends

# ── Scheduling & Async ────────────────────────────────────────────────────────
apscheduler==3.10.4
gevent==23.9.1
gevent-websocket==0.10.1

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.0

# ── Anthropic Claude (LLM layer) ──────────────────────────────────────────────
anthropic>=0.20.0
joblib==1.3.2
pytrends==4.9.2
